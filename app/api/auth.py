# Auth endpoints removed — SystemAssist is an extension service.
# Authentication is handled by the calling system via API key (X-Api-Key header).
# See app/security/rbac.py for the verify_api_key dependency.
